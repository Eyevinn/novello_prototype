extends layout

block content

  //form.upload-form(method="post", action = "/upload",enctype="multipart/form-data")
  //  input(type="file" name="upl" accept="video/*" capture="camera")
  // label.file-label(for="upl")
  //  input(type="submit")
