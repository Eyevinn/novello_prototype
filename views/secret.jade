extends layout

block content
  h1 Secret Area!
  a(href="/logout") logga ut

  form.upload-form(method="post", action = "/upload",enctype="multipart/form-data")
    input(type="file" name="upl")
    label.file-label(for="upl")
    input(type="submit")
